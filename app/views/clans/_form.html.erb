<fieldset>
  <legend>Clan Properties</legend>
  <%= f.label :name, 'Name' %><%= f.text_field :name %><br/>
  <%= f.label :crest %><%= f.file_field :crest  %>&nbsp;<em class="small">128x128 Image. Will be cropped and resized if larger.</em><br/>
  <%= f.label :server %><%= f.select :server, servers_select_options %><br />
  <%= f.label :access_type %><%= f.select :access_type, access_type_select_options %>
</fieldset>
<fieldset>
  <legend>Clan Description</legend>
  <%= markup_area 'clan', 'description' %>
</fieldset>
<fieldset>
  <legend>Clan Charter</legend>
  <%= markup_area 'clan', 'charter' %>
</fieldset>